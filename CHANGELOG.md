# Changelog

## 2.3.3 - 2026-09-04

- 仓库、源码目录和 `.spkg` 资产采用 STranslate 市场要求的 `STranslate.Plugin.Translate.Bailian` 命名。
- 保留原程序集名称和 `PluginID`，避免升级后丢失既有服务配置。

## 2.3.2 - 2026-08-29

- 修复 Coding Plan、Token Plan 和普通视觉模型无法被选作“图片翻译 OCR”服务的问题。
- 通用视觉 OCR 改为返回逐行文本与 `[0,999]` 归一化边界框，并按 STranslate 传入的图片尺寸转换为像素坐标。
- 保留非 JSON 模型输出的纯文字回退；已知不支持图像输入的模型仍不声明图片翻译能力。

## 2.3.1 - 2026-08-28

- 设置页增加明确的“保存设置”和“测试连接”操作及结果反馈。
- 设置页只读显示当前实际使用的 Base URL，并随计费模式和地域配置更新。
- 连接测试会先保存当前表单，再使用所选计费模式、模型、端点和 API Key 发起非流式最小请求。
- 连接错误继续隐藏 API Key，避免在界面消息中泄露凭据。
- 修复 STranslate 逐行返回 SSE 时把相邻事件错误拼接、导致流式翻译提示无结果文本的问题。

## 2.3.0 - 2026-08-28

- 首次发布 STranslate 百炼单包插件，同时提供翻译和 OCR。
- 支持按量付费、Coding Plan 与 Token Plan，默认按量付费和 `qwen3.7-plus`。
- 支持普通与 Qwen-MT 翻译、SSE 流式输出、取消、思考参数及错误脱敏。
- 支持通用视觉文字 OCR，以及按量 Qwen OCR 的 `location` / `rotate_rect` 像素坐标。
